# Student Dropout Risk Ranking

Ranks enrolled students by dropout risk at the start of semester 2, so a
capacity-constrained support programme reaches the 10% most likely to need it.

## Quick start

```bash
make setup    # uv venv + pinned deps
make stack    # zenml init, register MLflow tracker/registry
make test
make train
```
